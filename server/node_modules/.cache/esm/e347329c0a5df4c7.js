let express,cors;_0a5‍.w("express",[["default",["express"],function(v){express=v}]]);_0a5‍.w("cors",[["default",["cors"],function(v){cors=v}]]);

const morgan = require("morgan");
const { readdirSync } = require("fs");
const mongoose = require("mongoose");
require("dotenv").config({ path: "./config.env" });

const app = express();

app.use(cors());
app.use(express.json());
app.use(morgan("dev"));

readdirSync("./routers").map((r) => {
  app.use("/api", require(`./routers/${r}`));
});

_0a5‍.g.console.log(process.env.DB);

mongoose
  .connect(process.env.DB, {
    useNewUrlParser: true,
    useUnifiedTopology: true,
  })
  .then(() => console.log("DB connection successfully"))
  .catch((err) => _0a5‍.g.console.log(err.message));

const port = 8000;

app.listen(port, () => {
  _0a5‍.g.console.log("listening on the port " + port);
});
